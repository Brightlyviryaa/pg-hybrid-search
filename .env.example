# PostgreSQL Connection (Required)
# Replace with your local/production connection string
DATABASE_URL=postgresql://username:password@localhost:5432/your_database

# OpenAI Configuration (Required for embeddings)
# Get a key from https://platform.openai.com/
OPENAI_API_KEY=sk-your-openai-api-key

# Optional: Override default embedding model
# Default: text-embedding-3-small
EMBED_MODEL=text-embedding-3-small

# Voyage AI Configuration (Optional - for reranking)
# Get a key from https://www.voyageai.com/
VOYAGE_API_KEY=pa-your-voyage-api-key

# Optional: Override default rerank model
# Recommended defaults: rerank-2.5 or rerank-2.5-lite
RERANK_MODEL=rerank-2.5-lite

# Testing (Optional)
# Used only by local tests; not required in production
# TEST_DATABASE_URL=postgresql://test:test@localhost:5432/test_db

# Usage:
# 1) Copy this file to .env
# 2) Fill in real credentials
# 3) Run: npx @brightly/pg-hybrid-search init
